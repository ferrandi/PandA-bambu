#this is file scripts/Makefile.am
SUBDIRS=
DIST_SUBDIRS=
if BUILD_MPPB
   SUBDIRS += mppb
   DIST_SUBDIRS += mppb
endif

DOX_TAGFILES =

EXTRA_DIST = add_escape.sh test_panda.py test_installation.sh

#do not touch the following line
include $(top_srcdir)/panda.am

